# Project-local Node runtime. pnpm downloads this version and uses it for all
# pnpm run / pnpm exec / pnpm node calls, independent of the global node.
# LTS line only: dependency-cruiser refuses odd-numbered releases.
use-node-version=24.21.0
engine-strict=true
