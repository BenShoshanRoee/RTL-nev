[project]
name = "rtlenv"
version = "0.0.0"
description = "RTL environments bench layer: control, judge, tasks, runner, metrics."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["uv_build>=0.10,<0.13"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "rtlenv"
module-root = ""
